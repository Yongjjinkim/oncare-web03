# Step 1: 프로젝트 폴더 구조 생성 및 유저용(/onstart) 라우트 설정

현재 `ONCARE-WEB03` 프로젝트의 `src/app` 폴더 내부에 아래와 같이 새 폴더와 파일을 생성합니다.

## 1. 생성할 디렉토리 구조

```text
src/
└── app/
    ├── onstart/
    │   └── page.tsx      <-- 유저 검진지 업로드 페이지
    └── admin/
        └── page.tsx      <-- 간호사 QC Gate 대시보드 페이지
```
